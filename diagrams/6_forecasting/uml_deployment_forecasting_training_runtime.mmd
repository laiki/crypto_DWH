flowchart LR
    subgraph HOST["Execution Host (VM/Server)"]
        SCHED["Scheduler / Cron"]
        TRAINJOB["Forecast Training Job<br/>(train_model_registry.py)"]
        TMP["Ephemeral Memory Workspace<br/>(per-series resampled frame)"]
    end

    subgraph DATA["Data Layer"]
        STAGEDB["Staging DB Files<br/>(data/staging/*.db)"]
        CLEANDB["Cleansing DB<br/>(data/cleansing/*.db)"]
        REGDB["Forecast Registry DB<br/>(data/forecasting/forecasting_registry.db)"]
        MODELSTORE["Model Artifact Store<br/>(data/forecasting/models/)"]
    end

    SCHED --> TRAINJOB
    TRAINJOB --> STAGEDB
    TRAINJOB --> CLEANDB
    TRAINJOB --> TMP
    TRAINJOB --> REGDB
    TRAINJOB --> MODELSTORE
